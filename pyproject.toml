[project]
name = "chameleon"
version = "0.1.0"
description = "Native media format converter powered by an FFmpeg-first Python worker."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["python"]
