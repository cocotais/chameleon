[project]
name = "winui-python-ai-media-starter"
version = "0.1.0"
description = "WinUI3 plus Python stdio JSON-RPC AI multimedia starter."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["python"]

